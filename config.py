# ================== DeepSeek API配置 ==================

# 从 https://platform.deepseek.com/ 获取API密钥
DEEPSEEK_API_KEY = "这是key"



# DeepSeek API地址
DEEPSEEK_BASE_URL = "https://api.deepseek.com"

# 模型选择：deepseek-chat(便宜+联网) / deepseek-reasoner(推理强)
DEEPSEEK_MODEL = "deepseek-chat"

# 是否启用联网搜索（True=联网搜答案更准，但略慢）
DEEPSEEK_ENABLE_SEARCH = True

# ================== 屏幕捕获配置 ==================

# 默认捕获区域（后面用工具重新框选）
CAPTURE_REGION = {
    "left": 100,
    "top": 100,
    "width": 800,
    "height": 600
}

# 快捷键
HOTKEY_SELECT = "ctrl+f1"    # 框选区域
HOTKEY_PAUSE = "ctrl+f2"     # 暂停/继续
HOTKEY_CAPTURE = "ctrl+f3"   # 手动触发一次识别
HOTKEY_EXIT = "ctrl+q"       # 退出

# 扫描间隔（秒）
SCAN_INTERVAL = 1.0


# ================== 本地知识库 ==================
#
# 作用：OCR 识别出题目后，先在这里查找；命中即直接返回答案（毫秒级、不调用 API）。
#       未命中才会去调用 DeepSeek API（联网搜索）兜底。
#
# 每条格式：
#   "条目名称（唯一标识，可随意起名）": {
#       "keys":   ["关键词1", "关键词2", ...],  # 匹配触发词：题目文本里只要包含【任意一个】关键词即命中
#       "answer": "A",                          # 答案：
#                                               #   选择题 → 选项字母，如 "A" / "B" / "A,C"
#                                               #   判断题 → "正确" 或 "错误"
#                                               #   填空题 → 直接填应填内容，如 "北京" / "H2O"
#       "detail": "简要解析说明",                 # 可选：命中后显示的解释（不写也行）
#   },
#
# 匹配规则（重要）：
#   1. 题目文本【包含】 keys 中任意一个关键词即可命中。
#   2. 若多条同时命中，程序自动取【命中关键词最多】的那条，减少误判。
#   3. 匹配时会自动忽略空格与常见标点、不区分大小写，对 OCR 的识别噪声更鲁棒。
#
# 使用技巧：
#   1. 把要刷的题库逐条录入，可大幅减少 API 调用、降低延迟。
#   2. 关键词要“独特且短”，例如“四大发明”“水的化学式”；不要用“的”“是”“什么”这类通用字。
#   3. 同一题可写多个同义/变体关键词，例如 ["阿伏伽德罗常数", "阿伏加德罗常数", "阿佛加德罗常数"]。
#
LOCAL_KNOWLEDGE = {
    # ---- 示例 1：选择题（answer 填选项字母）----
    "四大发明": {
        "keys": ["四大发明", "古代四大发明"],
        "answer": "A",                          # 假设 A 选项是“造纸术、印刷术、火药、指南针”
        "detail": "四大发明：造纸术、印刷术、火药、指南针。",
    },

    # ---- 示例 2：判断题（answer 填“正确/错误”）----
    "光速是否为最快速度": {
        "keys": ["光速是最快的速度", "真空中光速最快", "光速能否被超越"],
        "answer": "正确",
        "detail": "真空中的光速约为 30 万千米/秒，是目前已知最快的速度。",
    },

    # ---- 示例 3：填空题（answer 直接填内容）----
    "水的化学式": {
        "keys": ["水的化学式", "水的分子式"],
        "answer": "H2O",
        "detail": "水由两个氢原子和一个氧原子构成，化学式为 H2O。",
    },
    "中国首都": {
        "keys": ["中国的首都", "我国首都", "共和国的首都"],
        "answer": "北京",
        "detail": "中华人民共和国的首都是北京。",
    },
    "勾股定理": {
        "keys": ["勾股定理", "直角三角形两直角边"],
        "answer": "a² + b² = c²",
        "detail": "直角三角形两直角边的平方和等于斜边的平方。",
    },

    # ---- 在此继续添加你自己的题目（复制上面的结构，替换内容即可）----
    # "你的题目名称": {
    #     "keys": ["关键词1", "关键词2", "关键词3"],
    #     "answer": "A",
    #     "detail": "简要解析说明",
    # },
}


# ================== 本地题库文件配置 ==================
KB_PATH = "1.csv"                     # 题库文件（相对项目目录或绝对路径，支持 .csv / .xlsx）
KB_QUESTION_COL = "题目"              # 题干列的表头名
KB_ANSWER_COL = "正确选项+答案"       # 答案列的表头名（会自动匹配到「正确选项+答案（必填）」）
KB_DETAIL_COL = ""                    # 解析列的表头名（没有解析列就留空 ""）
KB_MIN_RATIO = 0.55                   # 模糊匹配相似度阈值（0~1，越大越严格）
